# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FireBasePost' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FireBasePost
pod "PGFramework"
pod 'Firebase/Analytics'

  target 'FireBasePostTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FireBasePostUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
